import { SlangHiderPipe } from './slang-hider.pipe';

describe('SlangHiderPipe', () => {
  it('create an instance', () => {
    const pipe = new SlangHiderPipe();
    expect(pipe).toBeTruthy();
  });
});
